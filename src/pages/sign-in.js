import Head from 'next/head';

/**
 * Sign-in UI comes from routesConfig + SignInConfig inside FuseLayout (useRoutes).
 */
export default function SignInRoute() {
  return (
    <>
      <Head>
        <title>Sign in</title>
      </Head>
    </>
  );
}
