import Head from 'next/head';

/**
 * Next needs a pages file for this URL; Fuse `useRoutes` renders `Orderlist` (routesConfig).
 */
export default function ActivatesimsPage() {
  return (
    <>
      <Head>
        <title>Activate SIMs</title>
      </Head>
    </>
  );
}
