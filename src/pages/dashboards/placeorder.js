import Head from 'next/head';

/** Content from Fuse `useRoutes` (routesConfig). */
export default function PlaceorderPage() {
  return (
    <>
      <Head>
        <title>Place order</title>
      </Head>
    </>
  );
}
