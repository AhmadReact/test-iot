import { useSelector } from 'react-redux';

const BritexAuthoriztion = () => {
  const { user } = useSelector(() => {});

  return <div>BritexAuthoriztion</div>;
};

export default BritexAuthoriztion;
